# deps
node_modules/
# lib/ 必须入库：DSH 走 GitHub 固定源安装且契约禁止安装期构建脚本，
# 不提交产物 = 装到空壳（见 dsh-plugins/AGENTS.md「固定源发布」）。
*.tsbuildinfo
*.tgz

# tooling
.pnpm-store/
npm-debug.log*

# os
.DS_Store
Thumbs.db

# editor
.idea/
.vscode/
*.swp

# 本机 junction relink 记录（路径指向本机 .pnpm store，不入库）
.alpha4-relink.json
